# GitHub Actions Secrets Configuration
# Copy this file and add the values to your GitHub repository secrets

# === Required for Build & CI ===
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key-here

# === Required for Supabase Functions Deployment ===
SUPABASE_ACCESS_TOKEN=your-personal-access-token
SUPABASE_PROJECT_REF=your-project-ref-id

# === Optional - Stripe Integration (for Edge Functions) ===
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# === How to Add These to GitHub ===
# 1. Go to your repository on GitHub
# 2. Settings → Secrets and variables → Actions
# 3. Click "New repository secret"
# 4. Add each variable with its value

# === Where to Find Values ===
# VITE_SUPABASE_URL & VITE_SUPABASE_PUBLISHABLE_KEY:
#   → Supabase Dashboard → Project Settings → API

# SUPABASE_ACCESS_TOKEN:
#   → https://supabase.com/dashboard/account/tokens
#   → Click "Generate new token"

# SUPABASE_PROJECT_REF:
#   → Supabase Dashboard → Project Settings → General
#   → Look for "Reference ID"

# STRIPE_SECRET_KEY & STRIPE_WEBHOOK_SECRET:
#   → Stripe Dashboard → Developers → API Keys
#   → Stripe Dashboard → Developers → Webhooks
