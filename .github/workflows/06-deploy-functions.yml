name: "06 - Deploy Supabase Functions"

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

jobs:
  deploy:
    name: Deploy to ${{ inputs.environment }}
    runs-on: ubuntu-latest
    environment:
      name: supabase-${{ inputs.environment }}

    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy functions
        run: |
          PROJECT_REF=$(grep 'project_id' supabase/config.toml | head -1 | cut -d'"' -f2)
          echo "Conectando ao projeto Supabase: $PROJECT_REF"
          supabase link --project-ref "$PROJECT_REF"
          FUNCTIONS=$(ls -d supabase/functions/*/ | xargs -n 1 basename)
          for func in $FUNCTIONS; do
            echo "Implantando função: $func"
            supabase functions deploy $func --no-verify-jwt
          done
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - run: |
          echo "## 🚀 Functions Deployed" >> $GITHUB_STEP_SUMMARY
          echo "Environment: ${{ inputs.environment }}" >> $GITHUB_STEP_SUMMARY
          echo "**Next:** Stage 7 - Deploy Environment" >> $GITHUB_STEP_SUMMARY
